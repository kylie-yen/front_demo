# 路安智养前端设计系统

本项目的界面语言来自 `template.fig` 中的 Workroom 后台模板，并结合道路主动养护业务重新组织。设计稿只作为视觉与组件参考，项目原有业务数据、路由和交互逻辑保持不变。

## 视觉方向

- 工作区使用浅冰蓝背景，内容以纯白大圆角面板承载。
- 主色为亮蓝，标题为深海军蓝；成功、告警、风险分别使用绿色、橙色和红色。
- 采用左侧分组导航、顶部轻量工具栏以及卡片化业务内容。
- 阴影保持克制，主要依赖色块、留白和层级表达信息结构。

## 核心令牌

| 用途 | 令牌 | 值 |
| --- | --- | --- |
| 品牌主色 | `--blue-500` | `#3D8DFF` |
| 标题文字 | `--navy-950` | `#10213B` |
| 正文文字 | `--slate-600` | `#60748C` |
| 工作区背景 | `--workspace` | `#F3F8FD` |
| 成功 | `--success` | `#33C694` |
| 告警 | `--warning` | `#FFAD43` |
| 风险 | `--danger` | `#F16062` |
| 卡片圆角 | `--radius-lg` | `22px` |

## 响应式约定

- `>1180px`：完整双栏看板与四列指标。
- `861–1180px`：单栏业务内容、两列指标。
- `≤860px`：侧栏变为抽屉导航。
- `≤620px`：单列指标、筛选条件自动换行，宽表保留横向滚动。

动画遵循 `prefers-reduced-motion`，交互元素保留清晰的键盘焦点状态。
